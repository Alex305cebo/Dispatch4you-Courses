# Firebase sign-in helper files — НЕ трогать их EOL.
# Это файлы, скачанные как есть с dispatch4you-80e0f.firebaseapp.com
# и должны остаться байт-в-байт такими же (иначе JS может сломаться
# или helper перестанет работать).
* -text
