# Extension build tooling — secret signing key + packer, local only (NOT deployed)
build/
installer/Dispatch4you-Setup.exe

# Dependencies
node_modules/
package-lock.json
# Исключение: map-trainer нужен для GitHub Actions сборки
!map-trainer/package-lock.json
# Исключение: dispatch-academy-app нужен для GitHub Actions сборки
!dispatch-academy-app/package-lock.json

# Database
users.db
*.db
*.db-journal

# Environment variables
.env
.env.local
.env.production

# Credentials (ВАЖНО: не коммитить!)
credentials.json
load-finder-app/credentials.json

# FTP/SFTP credentials
.vscode/sftp.json
sftp.json
deploy-to-hostinger.ps1
upload-simple.ps1

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS files
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
!.vscode/settings.json
.idea/
*.swp
*.swo
*~

# Backups — не нужны в репо
# ВАЖНО: паттерн *.bak* со звёздочкой в конце. Просто *.bak НЕ ловит .bak2/.bak3 —
# из-за этого три css/*.bak2 попали в репо и уехали на прод в открытый доступ.
*.backup
*.bak*
*.old
*.albers-backup
*.fixed-projection
*.v2-debug
.backups/
backup_*/

# Build files
dist/
build/

# Temporary files
tmp/
temp/
*.tmp

# Archives
*.zip

# Session reports
SESSION-*.md

# Test files
test-*.html
*-test.html
game-test.html
MAP_DEBUG_INSTRUCTIONS.html

# Dev-only scripts
*.py
*.ps1
states_extracted.txt

# Old/unused modules
Old Modules/

# ffmpeg binaries (local tool, not for deploy)
adventure/ffmpeg-bin/
adventure/ffmpeg.zip
adventure/filelist.txt

# Video files (uploaded manually to server)
adventure/assets/*.mp4
adventure/*.mp4

# graphify: локальный knowledge graph — только для ассистента, НЕ коммитим/не деплоим
graphify-out/

# ── Локальные рабочие материалы и тулинг (НЕ для деплоя) ──
.cursor/
.codex/
__pycache__/
3.14.4_0/
# Слэш в начале обязателен — иначе шаблон ловит папку с таким именем на ЛЮБОМ
# уровне, а git на Windows ещё и регистронезависим (core.ignorecase=true).
# Без него это правило молча съедало assets/video/ и home-v2/video/: файлы
# добавляли, git их тихо игнорировал, до сервера они не доезжали (так пропали
# ролики explore-loadboard-* и explore-deal-*). Ловим только корневую Video/
# с сырыми исходниками.
/Video/
promo-video/
remotion/
Coworker/
Beta Hero sec/
img/Carusel 6x6/

# ── Конфиги/контекст ассистентов (локальные, у каждого свои) ──
.claude/CLAUDE.md
.claude/skills/
.graphifyignore
AGENTS.md
CLAUDE_PROJECT_CONTEXT.md

# ── Черновики и дубликаты (не для деплоя) ──
/landing-hero-left.html
/landing-mindloop.html
assets/img/ml-avatar-*.svg
games/collection/
# Survivors деплоится отдельным flow из gh-pages — копия на main не нужна
games/Survivors/
voice-to-chat-extension/CLAUDE_CODE_GUIDE.md
