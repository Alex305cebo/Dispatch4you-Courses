# DEPRECATED — replaced by deploy.yml
# This file is kept for reference only and will not run.
name: DEPRECATED - Build & Deploy Game to GitHub Pages

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'This workflow is deprecated. Use deploy.yml instead.'
        required: true

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. See deploy.yml"
